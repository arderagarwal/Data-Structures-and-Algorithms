#include<bits/stdc++.h>
using namespace std;
int main(){
    queue<int> q;
    //inserting element into queue
    q.push(1);
    //removing element from queue
    q.pop();
    //finding element at front of queue
    q.front();
}
